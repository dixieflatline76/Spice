package config

// AppVersion is the version of the service.
const AppVersion = "v0.1.0" // Or get it from version.txt during build

// ServiceName is the name of the service.
const ServiceName = "Spice"
